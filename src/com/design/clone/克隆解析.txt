java中的值传递和对象传递：
java中的值传递就是指方法调用是传递的对应形式的形式参数，而不是实际参数，也就是说当参数的属性发生改变时，原来传递的参数并不会发生改变
java中的对象传递就是将原来的参数对象的实参直接传入，当里面的属性发生改变时，原有的参数的属性也会相应发生变化
java都是值传递

　　【浅克隆】，通常只是对克隆的实例进行复制，但里面的其他子对象，都是共用的。

　　【深克隆】，克隆的时候会复制它的子对象的引用，里面所有的变量和子对象都是又额外拷贝了一份。